package com.first.project.repositories;

import com.first.project.entities.Request;
import org.springframework.data.jpa.repository.JpaRepository;

public interface repository_Request extends JpaRepository<Request,Integer> {
}
